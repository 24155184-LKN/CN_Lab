<?xml version="1.0" encoding="UTF-8"?>
<cherrytree>
  <node unique_id="17" master_id="0" name="TCP_Client.c" prog_lang="c" tags="" readonly="0" nosearch_me="0" nosearch_ch="0" custom_icon_id="0" is_bold="0" foreground="" ts_creation="1787029920" ts_lastsave="1787030100">
    <rich_text>// Send DOMAIN NAME to Server

#include &lt;stdio.h&gt;
#include &lt;stdlib.h&gt;
#include &lt;string.h&gt;
#include &lt;unistd.h&gt;
#include &lt;arpa/inet.h&gt;

#define PORT 8082
#define MAX 100

int main() {
    int sock;
    struct sockaddr_in server_addr;

    char domain[MAX];
    char ip[MAX];

    // Creating SOCKET
    sock = socket(AF_INET, SOCK_STREAM, 0);

    // Server address
    server_addr.sin_family = AF_INET;
    server_addr.sin_port = htons(PORT);
    server_addr.sin_addr.s_addr = inet_addr("127.0.0.1");

    // Connect
    connect(sock, (struct sockaddr *)&amp;server_addr,
            sizeof(server_addr));

    printf("Connected to Server!\n");

    // Input domain
    printf("Enter domain name: ");
    scanf("%s", domain);

    // Send domain
    send(sock, domain, sizeof(domain), 0);

    // Receive IP
    recv(sock, ip, sizeof(ip), 0);

    printf("IP Address: %s\n", ip);

    close(sock);

    return 0;
}
</rich_text>
  </node>
</cherrytree>
